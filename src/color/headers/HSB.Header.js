zetaret.global.packages.clockwork.color.HSB.__xeltoss_prepare = function(x) {
	x.addEmbedMap(this, {
		Hue: x.argumentKeyMatch(0, 0),
		Saturation: x.argumentKeyMatch(0, 0),
		Brightness: x.argumentKeyMatch(0, 0)
	});
};